# Servidor MCP (Marco C4) — fino de propósito: SDK + cliente HTTP, nada de Django.
# Faixa (não pin exato): o SDK evolui rápido e só usamos a superfície estável
# (FastMCP + streamable-http, >= 1.9).
mcp>=1.9,<2
httpx>=0.27
